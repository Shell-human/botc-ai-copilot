# 发布前冒烟测试清单 (Release Smoke Test Checklist)

> 适用于 `test` 分支每次提交前的快速手动验证。全部通过后可合并到 `main` 发布。

## 基础加载

- [ ] 首次加载页面无 console 错误（F12 → Console 无红字）
- [ ] Lucide 图标正常渲染（骷髅头、语言切换图标可见）
- [ ] 所有 CSS/JS 文件返回 200（Network 面板无 404）

## 新对局初始化

- [ ] 点击「初始化并开启新游戏」→ 座位图正确生成
- [ ] 玩家列表显示对应人数
- [ ] 流水志显示初始化日志（剧本名、座位、角色、阵营）
- [ ] 剧本预览弹窗内容正确

## API 厂商与状态指示器

- [ ] 切换厂商为 DeepSeek → 右上角状态指示器显示「DeepSeek 已就绪」
- [ ] 切换厂商为 ChatGPT → 状态指示器显示「ChatGPT Ready」
- [ ] 刷新页面（Cmd+R）→ 厂商选择保持、状态指示器正确、API Key 保持
- [ ] 切换回 Gemini → 模型下拉框更新为 Gemini 模型列表

## 持久化存储

- [ ] 选择 DeepSeek + 输入 API Key → 刷新页面 → 仍选 DeepSeek、密钥仍在
- [ ] 选择邪恶阵营 + 设置 3 个说书人伪装 → 刷新页面 → 伪装下拉框值保持
- [ ] 输入控制台文本 → 刷新页面 → 输入框草稿保持
- [ ] AI 分析一次后 → 刷新页面 → 分析结果 tab 内容保持

## 语言切换

- [ ] 中→英切换：下拉框文本、按钮文本、标题全部变为英文
- [ ] 英→中切换：同上，全部变回中文
- [ ] 语言切换后状态指示器文字正确（不固定显示 Gemini）
- [ ] 语言切换后 evilBluffs 下拉框选中值保持

## 剧本切换

- [ ] 切换剧本 → 我的角色下拉框更新为新剧本角色
- [ ] 切换剧本 → 剧本预览弹窗更新
- [ ] 切换剧本 → 大盘逻辑校验器更新
- [ ] 切换剧本 → evilBluffs 下拉框选项更新为好人角色

## 玩家弹窗

- [ ] 点击座位图节点 → 弹窗打开，显示该玩家信息
- [ ] 修改角色/阵营/存活状态 → 保存 → 座位图、玩家列表、流水志同步更新
- [ ] 勾选中毒/醉酒 → 座位图节点样式变化
- [ ] 关闭弹窗 → 数据保持

## 移动端响应式

- [ ] 375px 宽度下三栏纵向排列
- [ ] 所有按钮可正常点击
- [ ] 座位图缩放适配
- [ ] 弹窗不超出屏幕

## AI 分析

- [ ] 输入局势文本 → 点击发送 → API 调用成功
- [ ] 响应正确分发到「即时分析」「平行世界线」「行动建议」三个 tab
- [ ] 思维链展开/折叠正常
- [ ] Markdown 渲染正常（标题、列表、加粗）

## 对话模式

- [ ] 切换对话模式开关 → 输入框 placeholder 变为对话提示
- [ ] 发送按钮文字变为「发送对话提问」
- [ ] 对话模式下输入不记入流水志
- [ ] 切换回分析模式 → UI 恢复
